% This file compiles with both LuaLaTeX and XeLaTeX
\documentclass[11pt]{article}

% Change "review" to "final" to generate the final (sometimes called camera-ready) version.
% Change to "preprint" to generate a non-anonymous version with page numbers.
\usepackage[review]{acl}
% This is not strictly necessary, and may be commented out,
% but it will improve the layout of the manuscript,
% and will typically save some space.
% \usepackage{microtype}

% If the title and author information does not fit in the area allocated, uncomment the following
%
%\setlength\titlebox{<dim>}
%
% and set <dim> to something 5cm or larger.

% These font selection commands work with
% LuaLaTeX and XeLaTeX, but not pdfLaTeX.
\usepackage[english,bidi=default]{babel} % English as the main language.
\babelfont{rm}{TeXGyreTermesX} % similar to Times
%%% include whatever languages you need below this line
\babelprovide[import]{hindi}
\babelfont[*devanagari]{rm}{Lohit Devanagari}
\babelprovide[import]{arabic}
\babelfont[*arabic]{rm}{Noto Sans Arabic}


%\usepackage{polyglossia}
%\setdefaultlanguage{english}
%\setotherlanguages{arabic,russian,thai,hindi,kannada}
\usepackage{subcaption}
\usepackage{tikz}
\usepackage{linguex}
\usepackage{multicol}
\usepackage{amsmath, amssymb,stackengine,scalerel}
\usetikzlibrary{positioning}

%%%%%


\title{Learning Bias in Tonotactic Learning: Representations or Models}

% Author information can be set in various styles:
% For several authors from the same institution:
% \author{Author 1 \and ... \and Author n \\
%         Address line \\ ... \\ Address line}
% if the names do not fit well on one line use
%         Author 1 \\ {\bf Author 2} \\ ... \\ {\bf Author n} \\
% For authors from different institutions:
% \author{Author 1 \\ Address line \\  ... \\ Address line
%         \And  ... \And
%         Author n \\ Address line \\ ... \\ Address line}
% To start a seperate ``row'' of authors use \AND, as in
% \author{Author 1 \\ Address line \\  ... \\ Address line
%         \AND
%         Author 2 \\ Address line \\ ... \\ Address line \And
%         Author 3 \\ Address line \\ ... \\ Address line}

\author{First Author \\
  Affiliation / Address line 1 \\
  Affiliation / Address line 2 \\
  Affiliation / Address line 3 \\
  \texttt{email@domain} \\\And
  Second Author \\
  Affiliation / Address line 1 \\
  Affiliation / Address line 2 \\
  Affiliation / Address line 3 \\
  \texttt{email@domain} \\}

\begin{document}
\maketitle
\begin{abstract}
This paper investigates whether phonological learning primarily arises from the data representation (string vs. autosegmental representations) or from the learning model (probabilistic vs. categorical). We compare two learning frameworks: a Maximum Entropy (MaxEnt) model and BUFIA, a categorical learner adapted to operate over ARs. Using the same training and testing datasets across conditions, we systematically evaluate four combinations: MaxEnt-Ftr, BUFIA-Ftr, and BUFIA-AR. The results show that learning performance is strongest when grammars are induced over ARs, particularly when paired with a categorical learner. These findings suggest that representational structure plays a role in learning success than probabilistic weighting, and that categorical structure-sensitive learners benefit most from autosegmental representations.
\end{abstract}

\section{Introduction}

This paper focuses on the learning of tonal phonotactics, or tonotactics, which address the question of how tones and syllables (or other tone-bearing units, TBUs) are organized in a linguistically well-formed way in tonal languages. Despite the widespread distribution of tones in the world’s languages [cite], tonal phonotactics, as a core component of many languages, has not yet received the same level of attention as segmental phonology.

One possible reason for this gap may lie in representational differences. Segmental phonotactics is typically modeled using linear representations such as strings, which makes them readily applicable to a wide range of existing learning models. Tonotactics, on the other hand, has long been analyzed as depending on non-linear structures, such as autosegmental representations. This representational challenge has led to a relative scarcity of computational modeling of tones directly over non-linear structures, with many approaches instead flatten tonal forms into strings or features. Although there has been a great deal of work arguing for the inadequacy of linear representations in both the linguistic literature (for masking structural relations) and computational learning (where ARs may be more computationally efficient), the relevant works are still relatively limited. 

In this paper, we ask two questions: (1) how well tonotactics can be learned over ARs, and (2) how important representational differences are for learning compared with alternative string-based formats. To address this question, we adopt a recent version of the Bottom-Up Factorial Inference Algorithm (BUFIA) [cite], a categorical grammar induction algorithm that operates directly over ARs. We evaluate model performance and compare AR-based learning against alternative representations using the same algorithm as well as a Maximum Entropy (MaxEnt) learning algorithm [cite].

Specifically, we evaluate three conditions: string-based learning and AR-based learning, each learned by BUFIA-AR, or a probabilistic MaxEnt learner operating over strings. To ensure comparability, we convert the same Hausa dataset into two representations and use them as input to the models. We then compare model performance on held-out test data and constructed negative data. Admittedly, a complete study should also include MaxEnt operating over ARs. However, the current implementation of MaxEnt does not support learning directly from graphs. Despite this limitation, this comparison still allows us to assess whether a structural representation will contribute to learning.

The results show three main findings. First, tonotactics can be learned efficiently and effectively using BUFIA-AR, which deterministically returns a set of constraints consistent with the positive data. Some of the constraints learned by BUFIA align with previously established generalizations in the linguistic literature, while others reflect more specific conditions on the arrangement of tones and syllables. Second, we observe a learning difference between string-based and AR-based representations. When converted to ARs, the same BUFIA algorithm yields better recall and F1 scores, suggesting improved performance in recognizing positive data. Lastly, across models, categorical learning achieves better results than the probabilistic implementation in this task.

This work calls for greater attention to the use of linguistically meaningful representations in computational models of phonological learning.

\section{Backgrounds}
This section provides background on two related topics. First, it outlines the mathematical and computational preliminaries of linear and non-linear representations. Second, it introduces the workings of the Bottom-Up Factorial Inference Algorithm (BUFIA) and compares it with the Maximum Entropy (MaxEnt) framework.

\subsection{Linear vs Nonlinear Representation}
We begin by introducing two representations of tonal forms. In this work, 
segmental material is abstracted away, and tonal structure is represented 
in terms of tones, syllables, and moras. As shown in Table, two lexical 
items can be represented either as linear strings or as ARs. Some previous 
work also proposes tonal features (e.g., L.HL represented as [-H][+H, 
+contour] or [+L][+H, +contour]), but this system is not significantly 
different from using letters. Second, we assume the Obligatory Contour 
Principle (OCP), which prohibits adjacent identical tones, while allowing 
a single tone to spread across multiple units.
\input{reps.examples.tex}

In this section, we use Model Theory and view both representations as 
different mathematical objects. Different forms have distinct 
\textit{model signatures} that use formal logic to represent the 
relations of consisting units. 

A string can also be represented \textit{model-theoretically} 
with two components: the domain $D$ and a model signature of a finite 
set of relations $\mathcal{R}$. One can view $\mathcal{R}$ as a set of 
labeled nodes drawn from the alphabet, and a set of relations that capture 
the internal connections or ordering among those nodes. For example, a 
string model of \textit{L.HL} is shown in Figure~\ref{sec2.fig.string}.
Here, $D = \{1, 2, 3, 4\}$ indicates that there are four positions, 
among which $\{1, 4\}$ are labeled with $L$, $\{2\}$ with the boundary 
symbol $.$, and $\{3\}$ with $H$. These elements are connected via a 
binary successor relation, denoted by $\lhd$ in the signature, which 
specifies the linear order in which the symbols are pronounced.

\input{string,.mode.tex}
As one can observe, in a linear string model, the nodes are arranged along 
a single axis or tier, following a temporal order. Different than string 
models, we propose an AR model as follows the alphabet contains $\{H, L, 
\sigma, \mu\}$, where elements within each tier are in a successor 
relation, while elements across tiers stand in an association relation, 
denoted as $\alpha$. 
\input{AR.model.tex}

\subsection{Phonotactic Learning Models}
There are many string-based models that have been shown to be successful in 
phonotactic learning [cite]. One representative example is the Maximum 
Entropy (MaxEnt) phonotactic learner developed by [cite]. MaxEnt takes a 
set of positive data as input and returns a set of constraints assigned 
weights according to the principle of maximum entropy. It evaluates the 
well-formedness of words based on the sum of their weighted constraint 
violations. The higher the sum of constraint violateion, the less likely 
the candidate is considered as wellformed. 

Another approach, developed within formal language research, is categorical learning. Chandlee et al. \cite{chandleeLearningPartiallyOrdered2019} propose the Bottom-Up Factor Inference Algorithm (BUFIA), which discovers the most general forbidden subfactors represented in model-theoretic form and guarantees full coverage of the data space. BUFIA works by traversing a space of logically possible structures, moving from the most general (typically the empty structure) to more specific ones, and checking their presence in the positive data. If a structure is absent, BUFIA identifies it as a constraint of the language and prunes all of its superfactors, that is, structures that contain it. This procedure guarantees that the resulting grammar is the most general grammar consistent with the observed data.

Eith



\section{Experiment}
To test the tonotactic learnability over different representations and 
models, we conducted a computational experiment using the same Hausa data 
over two representational choices: string (features) vs. AR. For string 
representation, we ran two learning models: BUFIA and MaxEnt. Even though a 
two-by-two comparision will more comprehensive to see the results, the current
MaxEnt model is mainly based on linear representation and not readily opertaional for ARs, thgat beging said we ran the same experiment over a version of BUFIA. 

In this section, we will detail the experiemental setup, incvluding the two  
types of models and two tyrpes of representations, the testing and trianing data.

\subsection{Data}
In this case study, we adopted a small corpus of Hausa \citep{wold-4}, a tonal language spoken in West Africa, and evaluated the model by comparing the constraints it identified with previously established linguistic generalizations. Hausa has two basic tones, H and L, which can combine to form a falling contour (HL) when they occur on a single heavy syllable. However, monosyllabic LH contours and three-tone contours are not permitted. Previous research on Hausa tones has established the generalizations and rules summarized in Table \ref{rules} \citep{Newmanbook,Leben,zoll2003optimal}.

\input{constraints.table.tex}

A curated corpus was created from the Hausa entries in the \textit{World Loanword Database}. The corpus contains 621 monomorphemic, non-borrowed words
from the core vocabulary, transcribed in standard orthography. These forms are converted into both a string-based tonal representation and an autosegmental representation encoding tone–mora–syllable associations, which yield 61 unqie ones. Table 2 gives a two examples of such two representations. For example, the word \textit{kòò.gíí} `river' can be represented as a \textbf{\textit{string}} \textit{LL.HH}, which indicates the word has two bimoraic syllables: the first associated with L and the second with H. Contours will be decomposed into level tones, hence the falling tone in \textit{mù.tǔm} `person' becomes \textit{HL}. Under the string representation scheme, the alphabet contains $\{H, L, .\}$, and these symbols are ordered with a successor relation \citep{rogers2013}. The \textbf{\textit{graph representation}} of ARs abstracts the linguistic units and their relationships into \textit{nodes} and \textit{edges} \citep{jardine2017LocalNature}. In this case, the alphabet contains $\{H, L, \sigma, \mu\}$, where elements within each tier are in a successor relation, while elements across tiers stand in an association relation.

\input{reps.examples.tex}

\subsection{Models}
As mentioned above, we use two learning models: BUFIA and MaxEnt. Both models take as input a set of positive data and return a grammar. The two models differ in their learning mechanisms. BUFIA is a categorical learning model and has been implemented to operate over both autosegmental representations (ARs) and strings. MaxEnt, by contrast, is a statistical model that extracts constraints and assigns weights to them, evaluating forms based on their harmonic scores under a maximum entropy framework.

We performed four-fold cross-validation over the set of attested types. In each fold, the training set consists of 48 positive forms. The evaluation involves two test sets.

The first test set includes the remaining held-out positive data. To evaluate overgeneration, we constructed a matching set of negative forms. The sixteen negative forms each violate one or two pre-established constraints listed in Tables 1 and 2. As with the training data, the constructed negative forms are represented both as strings and as ARs (see Table X). Test Set 1 therefore contains 32 types in total (16 positive and 16 negative). Performance is evaluated by measuring whether the learned grammar correctly recognizes the well-formed positive data and rejects the negative forms.

The second test set builds on the first by adding four exceptional positive types, along with four matched negative forms, resulting in a total of 40 types. The exceptional forms are drawn from Hausa data reported by Newman and violate both *Clash and *Nonfinal-Fall. Newman notes that there are eight such examples in the corpus and suggests that they may have historical explanations, though the exact source of these exceptions remains unclear.

It should be noted that MaxEnt model gives a gradient grammar where constraints are all violable were assigned different weights. Howvever, to get a comparable 
results, we treat the Maxent retured constraints are all invoilatable. If any test form is considered to be well-formed by the Maxent if it does not violate any of the constraints. 



\section{Results}
The averaged results across folds are shown in Tables 4 and 5. For both test sets, all models under both representational formats achieved perfect precision (1.000). This indicates that, regardless of the learning mechanism, both string and AR representations successfully reject the constructed ill-formed structures.

By contrast, recall and F1 scores show greater variation. Across both test sets, the same performance pattern emerges: BUFIA-AR > BUFIA-string > baseline-string > MaxEnt-string. This ranking is consistent across held-out data and the extended test set including rare forms.
\begin{table}[t]
	\centering
	\begin{tabular}{l|ccc|ccc}
		\hline
		& \multicolumn{3}{|c|}{\textbf{Test Set 1}} & \multicolumn{3}{c}{\textbf{Test Set 2}} \\
		\hline
		\textbf{Model} 
		& \textbf{Recall} & \textbf{Prec.} & \textbf{F1}
		& \textbf{Recall} & \textbf{Prec.} & \textbf{F1} \\
		\hline
		baseline & 0.813 & 1.000 & 0.894 & 0.650 & 1.000 & 0.786 \\
		bufia-str    & 0.906 & 1.000 & 0.950 & 0.825 & 1.000 & 0.903 \\
		MaxEnt-str   & 0.641 & 1.000 & 0.774 & 0.525 & 1.000 & 0.680 \\
		bufia-ar     & 0.984 & 1.000 & 0.992 & 0.938 & 1.000 & 0.968 \\
		\hline
	\end{tabular}
	\caption{Averaged performance across four folds on Test Set 1 (16 held-out positives + 16 negatives) and Test Set 2 (20 positives, including rare forms, + 20 negatives). All models achieve perfect precision; differences reflect recall and F1.}
	\label{tab:results-merged}
\end{table}





\section{Discussion}
From the result, it can be seen that both models and representations play a role in tonotactic learning. 

For a static grammar, baseline-ar and basline string trun out to be idencal. This is because we can convert one to another, and at least in hausa there is not a case where two distinct forms from one system collopqse into the same in 
the other system. 

As for BUFIA-str/ar, there is a improvament when changing string into AR. On eht


One might question the result obtained by MaxEnt model since it is really low. One possible reason is the limited size of the data. The whole dataset is less than 70, and it wmight throw the model off. However, there are two things to mention. First, under a string representation, the complete alphabet and its all poisble combinations stringset under a fixed length is far smaller than a feature systems of segmental phonology. Secondly, we use types as the data, rather than tokens. But this should not be a big issue since the tonal patterns in Hausa is hightly skewed and falls into a zifps distribution: mroe than 60\% of the data have the same tonal structures. Given the a bigger dataset of with frequency, it will not do much help.
\section{Conclusion}



\end{document}
